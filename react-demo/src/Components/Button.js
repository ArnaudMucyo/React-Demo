import React from "react";
import './Button.css'

const Button = () => {

    return(
        <div>
            <button type="button" className="button">LOG IN</button>
        </div>
    )
}

export default Button;